#include "main.h"
/**
 * _puts_recursion - prints a string
 * @s: the string
 * main - check the code
 * Return: Always 0
 */
void _puts_recursion(char *s)
{
	char s;

	return (0);
}
int main(void)
{
	printf("The string is %d", s'\n');
	return (0);
}
